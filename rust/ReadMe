Reference: https://inpyjama.com/day-10-heap-vs-stack/

[]  Foreign Function Interface (FFI).
[]  Rust's central package repository, crates.io (think of it like Rust's version of PyPI)

create file name:
	cargo new program_name

build the repo:
	cargo build

run the repo:
	cargo run

clean the build:
	cargo clean
